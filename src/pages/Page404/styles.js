import styled from 'styled-components';

export const Page = styled.body`
    background: var(--white);
    padding: 0;
    margin: 0;
`;
export const Button404 = styled.a`
    background: var(--primary);
    color: #fff;
    padding: 15px 25px;
    border-radius: 15px;
`;